# huawei_ppt_master 资产候选补丁 v0.1

> 状态：候选补丁。当前未获得直接修改 Skill 授权，不写入 `skills/huawei_ppt_master/*`。

## 1. 候选补丁摘要

本补丁建议将本次生成PPT图片评审中可跨主题复用的规则沉淀到 Skill 资产，重点解决：

- 正文页红色底座过强；
- 多卡片同权平均主义；
- 案例页素材墙风险；
- 决策页表格吞没结论；
- 路线图缺少阶段门槛；
- 长标题不够压缩；
- 总结页缺少最终定义。

不沉淀本次 AI4MBSE 主题事实、论文术语、案例名称或未经核验数字。

## 2. `templates/visual_rules.md` 候选新增内容

```markdown
### 正文页红色底座控制规则

普通正文页不得让红色横条、红色底座或红色大色块成为页面最大视觉块。若红色块面积或视觉权重大于主图表/主结论，应降级为窄红线、浅灰底红色关键词或局部关键节点强调。

大面积红色底座仅适用于封面、章节页、强收口页或关键决策页。能力地图、架构图、路线图中，红色只能标记关键节点、路径门槛或最终结论，不得同时作为背景底座和多个模块强调色。
```

```markdown
### 多卡片主次层级规则

多卡片页必须显式区分主卡、支撑卡、注释卡。主卡最多 1 张，承担全页核心判断；支撑卡建议 2~4 张，承担依据、抓手或阶段动作；注释卡只承载边界、来源或补充说明，必须弱化显示。

目录页、核心结论页、路线图页不得只是平均分布卡片，必须体现阅读顺序、优先级、阶段门槛或管理收口。
```

## 3. `visual_patterns/layout_library.md` 候选新增内容

```markdown
### 阅读框架页语义约束

目录/阅读框架页不能只做章节导航，必须回答“高层应如何形成判断”。推荐使用“为什么—是什么—怎么落地—如何决策”或“判断—证据—路径—拍板”的递进结构，最后一格应承担管理收口。
```

```markdown
### 案例证明页语义约束

`left_logic_right_proof` 与 `metric_case_showcase_grid` 不得退化为图片墙。左侧必须给出判断链，右侧图片、截图、案例只证明一个核心判断。若证据不能支撑标题，应降级为待验证样例。
```

```markdown
### 决策建议页语义约束

`risk_decision_matrix` 页面中，拍板事项必须独立突出，表格只承载责任、资源、时点、产出和风险闭环。不得让表格吞没全页结论。
```

```markdown
### 路线图阶段门槛规则

`roadmap_timeline` 必须表达阶段递进，每阶段至少包含目标、关键动作、验收产出或进入下一阶段的门槛之一。不得只是并列计划。
```

## 4. `templates/chart_patterns.md` 候选新增内容

```markdown
### `case_gallery_cards` 失败条件

出现以下情况时，不应使用或应降级处理：

- 图片或案例数量多，但没有证明同一个标题判断；
- 右侧证据与左侧逻辑各讲一套；
- 案例页变成素材墙、Logo 墙或截图墙；
- 没有来源或未经核验的数字被当成事实。

触发时应降级为 `left_logic_right_proof` 中的单案例证明，或标注为待验证样例。
```

```markdown
### `decision_table` 失败条件

出现以下情况时，应重构页面：

- 表格占满主体区，拍板事项不突出；
- 表格只有原则，没有责任、资源、时点、产出；
- 高层看完仍不知道需要拍什么。

触发时应将页面改为“拍板事项卡 + 执行清单表”。
```

```markdown
### `capability_map` 失败条件

出现以下情况时，应降级或重构：

- 只列能力名称，没有连接到场景、机制或闭环；
- 所有能力同权，无法看出主能力和支撑能力；
- 图表无法解释标题中的关键判断。
```

## 5. `templates/wording_rules.md` 候选新增内容

```markdown
### 长标题压缩规则

当标题超过 28~32 个汉字且包含多个从句时，应优先压缩成强判断句。推荐句式：

- `不是 A，而是 B`
- `核心不在 A，而在 B`
- `要先完成 A，再推进 B`
- `真正瓶颈不是 A，而是 B`
- `从 A 走向 B，关键在 C`

压缩后，副标题或讲解口径再承载背景说明。
```

```markdown
### 总结页最终定义规则

总结页不应复述前文要点，而应形成最终定义或决策框架。推荐句式：

- `X = 对象 + 能力 + 治理边界`
- `X 的本质不是 A，而是 B`
- `最终要形成 A、B、C、D 四类可验收能力`
```

## 6. `eval/visual_scorecard.md` 候选新增内容

```markdown
### 新增一票降级项

- 普通正文页红色横条/底座成为页面最大视觉块：最高 B 档；
- 多卡片页所有卡片同权且无主次：最高 B 档；
- 案例页退化为图片墙/素材墙，无法证明标题判断：最高 B 档；
- 决策页表格吞没拍板事项，高层看不出需要决策什么：最高 B 档；
- 总结页只复述前文，没有最终定义或新增管理含义：扣 5~8 分。
```

## 7. 同步文件建议

如用户确认沉淀，需同步：

- `VERSION` → `0.3.5-generated-image-feedback`
- `CHANGELOG.md` 增加本次变更记录
- `README.md` 更新能力说明
- `INDEX.md` 更新资产索引
- `PACKAGE_MANIFEST.md` 更新文件清单与版本

## 8. 回归要求

- 不新增非法 `chart_type` / `layout_pattern`；
- 不改变默认完整交付链路；
- 不写入本次主题事实；
- 不引入无关 AI 算力/昇腾/NVIDIA 污染；
- 通过 `reference_learning_regression` 与 `visual_scorecard`。