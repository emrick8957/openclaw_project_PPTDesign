# changelog_draft

## v1.1 visual-assets proposed

### Added

- 建议新增华为式视觉降噪规则：红色锚点预算、正文页红色面积约束、底部结论条克制、卡片内边距与页脚安全区。
- 建议新增 layout 区域比例规则：标题区 10%-15%，主体区 75%-80%，页脚区约 5%，右侧洞察栏 25%-32%。
- 建议为 `key_findings_cards`、`comparison_table`、`swimlane_process`、`value_chain_loop` 增加组件级视觉语法。
- 建议扩展 `visual_scorecard.md`，增加红色克制、卡片层级、图表统一、留白安全区、底部结论条克制等评分项。
- 建议扩展 `generate_page_design.md`，每页输出 `red_anchor`、`card_hierarchy`、`spacing_rule`、`bottom_bar_rule`、`visual_simplification`。

### Changed

- 建议将 SWE Atlas 这类技术研究报告页，从“结构正确”升级为“华为风格视觉可控”：表格证据化、指标结论化、卡片分层、流程/闭环细线化。

### Fixed

- 针对 SWE Atlas 页面图片发现的红色过量、底部条压底、卡片同权、表格偏重、泳道/闭环节点不统一等问题，输出资产级 patch proposal。

### Status

- 当前仅为 proposal，不代表已修改 `huawei_ppt_master` Skill 文件。
- 建议人工确认后再进入资产修改阶段。
