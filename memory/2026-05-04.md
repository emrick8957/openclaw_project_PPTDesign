- 用户明确了 PPT 交付打包规则：允许总页数超过 10 页，但若超过 10 页，必须按页码拆成多个压缩包；例如 16 页时拆为 P1~P10 和 P11~P16 两个 zip，而不是把内容压缩到 10 页。
- 对《华为云竞争力深度洞察》这类交付，`A_to_B.md` 与 `status.md` 必须始终与当前有效版本和当前有效压缩包保持一致，废弃版本（如 v0.4）删除后要同步回滚交接状态。
- 用户会检查 `deck_spec.json` 的字段边界；`chart_type` 必须来自 `skills/huawei_ppt_master/templates/chart_patterns.md`，`layout_pattern` 必须来自版式库，不能把版式名误填到 `chart_type`。
- 当前用户对《华为云竞争力深度洞察》的关注点包括：高层经营视角、完整页数保留、按规则拆包交付，以及 deck_spec 字段定义的准确性。
