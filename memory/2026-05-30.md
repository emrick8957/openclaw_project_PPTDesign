# 2026-05-30 Durable Memory

## huawei_ppt_master chart_data visibility patch v0.2
- User requested revising `project/review/patch_proposals/chart_data_field_rules_and_visibility_patch_v0.1.md` to `chart_data_field_rules_and_visibility_patch_v0.2.md`.
- I created `project/review/patch_proposals/chart_data_field_rules_and_visibility_patch_v0.2.md` and did **not** modify formal `skills/huawei_ppt_master/*` assets.
- v0.2 target version: `v0.4.1-chart-data-visibility`.
- v0.2 absorbs prior review findings:
  - clarifies visible grouping fields vs logic-only `group`;
  - clarifies `edges.label` can be visible short action/relation text only, while complex direction/correspondence/layer/loop semantics must go to `chart_semantic_mapping`;
  - proposes adding `core/output_contracts.md` deck_spec strong gates;
  - adds phrase-length criteria for `label/name/headline/items/edges.label`;
  - clarifies `section` may only be weak header/chapter label, not body node/card/chart label;
  - adds RT-05~RT-08 regression cases.
- Boundaries preserved: no `page_render_spec`, no `normalized_render_model`, no rendering DSL, no `relation_type` or position/shape DSL fields in `chart_data`.
- Updated handoff/status files:
  - `project/handoff/B_to_A.md`
  - `project/handoff/status.md`
- Recommended next step: formally review v0.2; if approved, enter Phase 1 small-version landing for `v0.4.1-chart-data-visibility`.

- On 2026-05-30, user approved `chart_data_field_rules_and_visibility_patch_v0.2.md` and requested Phase 1 small-version landing. I formally upgraded `skills/huawei_ppt_master` to `v0.4.1-chart-data-visibility`, modifying `SKILL.md`, `core/deck_spec_field_dictionary.md`, `core/output_contracts.md`, `templates/chart_patterns.md`, `prompts/deck_spec_generation.md`, `eval/acceptance_checklist.md`, `eval/regression_cases.md`, `eval/visual_scorecard.md`, and synchronized README/VERSION/CHANGELOG/INDEX/QUICK_INDEX/PACKAGE_MANIFEST. Landing report: `project/review/review_reports/chart_data_visibility_phase1_landing_report_v0.1.md`; handoff/status updated. Core landed rules: `group`/`emphasis`/`source_status` are logic-only and must not be rendered as visible labels; visible grouping must use display fields such as `label/name/headline/display_text/lane.name/layer.name/stage.name`; `edges.label` is only short visible action/relation text; complex direction/correspondence/layer/loop semantics go to `chart_semantic_mapping`; `relation_type`, `edge_style`, `position`, `anchor`, `x/y`, `layer_index` and similar relationship/rendering DSL fields remain forbidden in `chart_data`. Validation passed: v0.4.1 markers present and no stale current-version markers in primary metadata. Suggested next step: Phase 2 dry-run with a sample deck_spec containing group, edges.label, section, and chart_semantic_mapping.
